{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/xulongwu4/pi-lazy-loader/blob/main/lazy-loader.schema.json",
  "title": "pi-lazy-loader configuration (standalone lazy-loader.json, or the \"lazy-loader\" object inside settings.json)",
  "type": "object",
  "required": ["packages"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "packages": {
      "type": "array",
      "items": {
        "oneOf": [
          { "type": "string", "minLength": 1 },
          {
            "type": "object",
            "required": ["source"],
            "additionalProperties": false,
            "properties": {
              "source": { "type": "string", "minLength": 1 },
              "commands": {
                "type": "array",
                "uniqueItems": true,
                "items": { "type": "string", "minLength": 1 }
              },
              "tools": {
                "type": "array",
                "uniqueItems": true,
                "items": { "type": "string", "minLength": 1 }
              },
              "guidelines": {
                "description": "Tool names whose promptGuidelines are injected into the system prompt while Pi's native tool list hides them (e.g. under pi-fabric); promptSnippet lines are always injected.",
                "type": "array",
                "uniqueItems": true,
                "items": { "type": "string", "minLength": 1 }
              }
            }
          }
        ]
      }
    }
  }
}
