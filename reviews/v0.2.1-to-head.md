# Code Review: `v0.2.1...HEAD`

- **Fixed point:** `v0.2.1` (`5be632a4cee20983f7b96a0d11204c3d1cb9a1cc`)
- **Reviewed HEAD:** `0ed1b0d` (`Add manifest-driven command proxies`)
- **Diff:** `git diff v0.2.1...HEAD`
- **Spec:** [`docs/command-proxy-development-requirements.md`](../docs/command-proxy-development-requirements.md)
- **Repository standards:** None found; Standards uses the Fowler smell baseline.

Commits reviewed:

```text
0ed1b0d Add manifest-driven command proxies
d0694a1 Allow shorthand command proxy declarations
c0850a2 Make command proxy descriptions optional
cc9ed7d Specify command description handoff
202605e Document v0.2.1 description handoff
```

## Standards

**Hard violations:** None; no repository-specific standards files exist.

**Baseline smells:**

- **Medium — Duplicated Code — `src/command-config.ts:64`, `src/command-config.ts:258`**: Manifest commands are independently traversed and validated during validation and merge, allowing rules to drift. Normalize once and merge the validated representation.
- **Medium — Divergent Change — `src/command-config.ts:109`, `src/command-config.ts:427`, `src/command-config.ts:488`**: One module owns schema validation, merge policy, filesystem loading, and display-description formatting. Move presentation formatting out of the configuration module.
- **Medium — Data Clumps — `src/command-config.ts:508`, `src/loader.ts:280`**: Package name, command name, descriptions, and target label travel together through overlapping object shapes. Use one shared command-description context.
- **Low — Duplicated Code — `checks/command-proxy-checks.ts:348`, `checks/command-proxy-checks.ts:482`, `checks/command-proxy-checks.ts:564`**: Three tests repeat temporary package creation, mock Pi construction, and cleanup. Extract one small fixture helper.
- **Low — Duplicated Code — `src/command-config.ts:12`, `src/manifest.ts:3`**: `CommandProxyDeclaration` is defined identically twice. Export and reuse one definition.
- **Low — Speculative Generality — `src/command-config.ts:279`, `src/command-config.ts:306`, `src/command-config.ts:313`**: `hasEmptyDesc` is maintained but never read. Remove it until merge behavior requires it.
- **Low — Primitive Obsession — `src/loader.ts:172`, `src/loader.ts:189`, `src/loader.ts:271`**: Command identity is repeatedly encoded as a colon-delimited string. Centralize key construction or use nested maps.

## Spec

### Missing or partial

- **High — MCP release gate absent.** Stage 2 requires “a cold TUI session [that] invokes each proxy,” including safe `/mcp-auth` execution (`Development Stages → Stage 2`). `checks/command-proxy-checks.ts` uses synthetic fixtures; `PHASE4-COMMAND-PROXY-RESULTS.md:84-90` documents only older v0.2.x `/token-burden` production evidence, not real `/mcp`, `/pi-mcp`, or `/mcp-auth` runs.

- **High — Performance/install release gates lack v0.3.0 evidence.** Stage 5 requires clean-pack installation, real TUI checks, and alternating A/B startup measurements; release acceptance requires packed/production TUI checks and positive interleaved A/B savings. `PHASE4-COMMAND-PROXY-RESULTS.md:84-90` instead records v0.2.x evidence and three absolute startup times—no clean-pack smoke, v0.3.0 TUI matrix, or per-package interleaved A/B comparison.

- **Medium — Shipped release documentation is incomplete.** Acceptance item 16 requires configuration, diagnostics, attribution limits, and rollback, while `Security Boundary` requires documenting the global-settings-only/project-override limitation. `README.md:98-104` only names `lazy-loader.json`; it omits its grouped format, diagnostics, canonical `sourceInfo` limitation, project-settings limitation, and rollback. The fuller requirements document is not included by the package allowlist in `package.json:10-16`.

### Scope creep

None found.

### Implemented but wrong

- **Medium — Packaging “allowlist” check is not exact.** The Test Matrix requires an explicit allowlist of published files and clean-install smoke. `checks/command-proxy-checks.ts:623-638` verifies only that expected entries are included and exist; extra published entries would pass, and no packed clean-install is exercised.

## Summary

**Standards:** 7 findings; worst issues are duplicated validation and the divergent responsibilities in `src/command-config.ts`. **Spec:** 4 findings; worst issues are the missing real MCP command matrix and missing v0.3.0 clean-install/performance release evidence.
